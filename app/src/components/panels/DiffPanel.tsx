/** Main-panel "Diff" tab placeholder; run-to-run diffing lands in a later phase. */
export function DiffPanel() {
  return (
    <p className="panel-empty" data-testid="diff-panel">
      Run diffing isn't implemented yet. See CHRONICLE_PLAN.md for the roadmap.
    </p>
  );
}
