# Copy this file to .env and fill in the values you need.
# Chronicle works with no configuration at all; these are optional overrides.

# Host/port the Chronicle server binds to.
CHRONICLE_SERVER_HOST=127.0.0.1
CHRONICLE_SERVER_PORT=7823

# Path to the SQLite database file used by the server.
CHRONICLE_DB_PATH=~/.chronicle/chronicle.db
